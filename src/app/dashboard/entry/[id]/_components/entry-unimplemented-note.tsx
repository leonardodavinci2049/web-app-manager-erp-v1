/**
 * Quiet hint rendered next to disabled actions while the corresponding
 * mutation is not implemented yet.
 */
export function EntryUnimplementedNote() {
  return (
    <p className="text-muted-foreground text-xs">Recurso não implementado</p>
  );
}
